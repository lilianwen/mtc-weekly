# MTC 技术动态周报 (2026-05-15 ~ 2026-05-22)

## 各仓库近期更新总结

### 仓库: ietf-plants-wg/merkle-tree-certs (IETF 草案)

此仓库是 MTC 协议草案的核心，近期更新主要集中在协议规范的细化、结构优化和移除实验性功能。

**技术上有意义的变动归纳：**

1. **移除实验性子树签名协议**：通过 PR #225 和 commit `c5a2556`，草案正式**移除**了先前定义的“临时子树签名协议”。这表明工作组决定简化协议，专注于核心的默克尔树证明机制，放弃了一个可能增加复杂性的可选扩展路径。

2. **引入并优化日志编号方案**：PR #213 和后续的 PR #233、#234 以及 issue #232 共同引入并完善了“日志编号”概念。核心变动是将证书和日志条目中的“树大小”字段拆分为两部分：**高 16 位表示日志编号，低 48 位表示条目在特定日志中的索引**（commit `6d2364a`）。这取代了之前无限/64位的拆分方案，为支持多日志操作（如跨日志的 CA）提供了更清晰的结构基础。相关 commit 还调整了文本，以支持多日志 CA 的场景。

3. **规范协同签名顺序**：PR #231 和 issue #221 解决了 `MTCProof` 中协同签名列表的排序问题。协议现在要求对协同签名进行**规范化排序**（例如按签名者密钥标识符排序），以确保证明的确定性，避免因顺序不同导致验证失败。

4. **增强 CA 和发布章节的说明**：PR #234 新增了专门针对**证书颁发机构**的章节，详细说明了 CA 在 MTC 系统中的角色和操作。PR #230 则修订了“发布”章节，使其描述更准确。这些更新使草案对实际部署的指导性更强。

5. **澄清协同消息验证细节**：PR #235 及相关 commit 扩展了如何填充和验证 `CosignedMessage` 的说明，减少了实现时的歧义。

**整体方向**：草案正在从概念框架转向更精确、可实现的规范。重点是**简化协议**（移除子树签名）、**增强可扩展性**（引入日志编号支持多日志）、**提高确定性**（规范协同签名顺序）以及**完善部署指南**（新增 CA 章节）。

---

## 重点关注清单

1. **子树签名协议被移除**：MTC 草案放弃了实验性的子树签名路径，表明协议设计正收敛于更核心的默克尔树证明机制。
2. **日志编号方案定型**：采用 16/48 位拆分方案来编码日志编号和条目索引，这是支持多日志、复杂 CA 操作的基础性变更。
3. **协同签名顺序规范化**：要求 `MTCProof` 中的协同签名列表按规范顺序排列，确保证明的唯一性和可验证性。
4. **CA 角色明确化**：草案新增专门章节详细说明 CA 在 MTC 系统中的职责和操作流程。
5. **协议细节持续澄清**：对 `CosignedMessage` 的验证等操作细节进行了补充说明，减少实现歧义。